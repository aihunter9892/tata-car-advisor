version: 1.0

runtime: python311

build:
  commands:
    pre-build:
      - pip install --upgrade pip setuptools wheel
    build:
      - pip install -r requirements.txt

run:
  runtime-version: "3.11"
  command: gunicorn app:app --bind 0.0.0.0:8080 --workers 2 --timeout 120
  network:
    port: 8080
  env:
    - name: PYTHONUNBUFFERED
      value: "1"